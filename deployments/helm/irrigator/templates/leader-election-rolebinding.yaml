apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "irrigator.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "irrigator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "irrigator.fullname" . }}-leader-election
subjects:
- kind: ServiceAccount
  name: {{ include "irrigator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
